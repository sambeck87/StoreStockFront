# URL base de la API backend (obligatoria en producción).
# Debe incluir el prefijo /api/v1. Todos los endpoints se resuelven contra esta URL.
# En desarrollo, si se omite, se usa el proxy de Vite (/api -> http://localhost:3000).
VITE_API_URL=http://localhost:3000/api/v1
